1. Add support for command line arguments
2. Add support for config
3. Add --all option to bump version number across all specified files in config
4. Add dry run
5. Write changes in the changelog file (version name, date, commit messages since the last version) # this requires much work
